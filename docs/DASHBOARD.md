# Dashboard Default Settings

This document describes the default configurations of the `dashboard` application, a user interface (`UI`) used to manage the settings and commands of the `IIoTGateway`.

## Default login:

- **username:** `admin`
- **password:** `gtwAdmin`